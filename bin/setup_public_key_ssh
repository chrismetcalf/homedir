#!/bin/sh

echo "Setting up public key ssh authentication"
if [ ! -f ~/.ssh/id_rsa.pub ]; then
  echo "Generating public/private key pair..."
  ssh-keygen -t rsa
fi

cat ~/.ssh/id_rsa.pub >> ~/.ssh/authorized_keys

echo "Done. Submit svn changes to commit"
