#!/bin/sh

PATH=$HOME/bin:$PATH

cd $HOME

echo "Setting up .ssh directory"
if [[ ! -d .ssh ]]; then
  echo ".ssh doesn't exist yet, creating..."
  mkdir .ssh 
fi
dircombine '.*' .ssh $HOME/.dotfiles/.ssh

echo "Linking in .dotfiles..."
dircombine '^\.' . $HOME/.dotfiles

echo "Done!"
