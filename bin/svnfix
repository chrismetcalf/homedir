#!/bin/sh

cd $HOME

echo "Linking in dotfiles..."
for fn in `find $HOME/.dotfiles -type f`; do
	if [ -f $fn ]; then
		echo "The file \"$fn\" already exists. Overwrite it? (Y/n)"
	fi
	
	echo "Symlinking $fn..."
done